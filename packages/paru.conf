[options]
PgpFetch
Devel
Provides
DevelSuffixes = -git -cvs -svn -bzr -darcs -always -hg -fossil

# scripts/configure-paru.py inserts the local [dotfiles] repository here.
# Paru Path does not expand ~ or $HOME, so the installed copy uses a resolved path.

[pkgbuilds]
Url = https://github.com/AkiraLyu/pkgbuilds.git
