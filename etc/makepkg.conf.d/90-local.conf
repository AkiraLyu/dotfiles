# 保留 pacman 提供的编译与安全选项，只替换本机指定的 CPU 架构。
# 此处延续原配置的 x86-64-v4；换机器时确认 CPU 支持，或删去这两行。
CFLAGS="${CFLAGS/-march=x86-64 /-march=x86-64-v4 }"
CXXFLAGS="${CXXFLAGS/-march=x86-64 /-march=x86-64-v4 }"

# 本机编译并行数、临时构建目录和打包者信息。
MAKEFLAGS="-j8 -l6"
BUILDDIR=/tmp/makepkg
PACKAGER="Akira <akira.uestc@gmail.com>"
